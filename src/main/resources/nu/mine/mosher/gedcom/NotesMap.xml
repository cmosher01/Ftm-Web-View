<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="nu.mine.mosher.gedcom.NotesMap">
    <select id="select" resultType="nu.mine.mosher.gedcom.Note" parameterType="nu.mine.mosher.gedcom.FtmLink">
        SELECT
            Note.NoteText AS note
        FROM
            Note
        WHERE
            Note.LinkTableID = #{table.id} AND
            Note.LinkID = #{id}
    </select>
</mapper>
